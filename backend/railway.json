{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "/backend/Dockerfile"
  },
  "deploy": {
    "startCommand": node -e "const pool = require('./database-pool'); pool.query('SELECT NOW()').then(r => console.log('DB SUCCESS:', 
  r.rows[0])).catch(e => console.error('DB ERROR:', e.message)); setTimeout(() => process.exit(0), 5000)",
    "healthcheckPath": "/health"
  }
}
