{
  "name": "dataroom-backend",
  "version": "1.0.0",
  "description": "AI-powered data room analyzer backend",
  "main": "server-final.js",
  "scripts": {
    "start": "npm run migrate && node server-final.js",
    "start:dev": "node server-final.js",
    "dev": "nodemon server-final.js",
    "test": "jest --testTimeout=30000",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:isolation": "jest tests/tenant-isolation.test.js",
    "test:integration": "jest tests/integration.test.js",
    "migrate": "node migrate.js",
    "setup:test-db": "node scripts/setup-test-db.js"
  },
  "dependencies": {
    "pg": "^8.11.0",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "express-rate-limit": "^6.10.0",
    "dotenv": "^16.3.1",
    "aws-sdk": "^2.1519.0",
    "multer-s3": "^2.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
